# Core MVP (working)
streamlit>=1.30.0
ultralytics>=8.1.0
opencv-python-headless>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0

# Phase 2 — heatmap + officer optimizer
folium>=0.15.0

# Phase 2 — tracking (lazy-imported; uncomment to enable in the app)
# deep-sort-realtime>=1.3.2

# Phase 2 — ANPR
# (lazy-imported; uncomment to enable in the app)
# PaddleOCR 3.x has a Windows-CPU executor bug, so pin <3.0.
# Paddle 2.x is also pinned to match.
# Newer torch (2.10+) has a shm.dll loading issue on Windows when
# imported after cv2; pin to a known-good range.
# paddleocr>=2.7.0,<3.0
# paddlepaddle>=2.6.0,<3.0
# torch>=2.4,<2.10
# torchvision>=0.19,<0.25

# Phase 3 — CRM classifier
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
# keplergl>=0.3.2

# Demo / live-stream support (optional)
# yt-dlp>=2024.1.0  # optional, for YouTube stream testing in RTSP tab
