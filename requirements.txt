# Core MVP (working)
streamlit>=1.30.0
ultralytics>=8.1.0
opencv-python-headless>=4.8.0
pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0

# Phase 2 — heatmap + officer optimizer (no extra deps for optimizer)
folium>=0.15.0

# Phase 2 — tracking (lazy-imported; uncomment to enable in the app)
# deep-sort-realtime>=1.3.2

# Phase 2 — ANPR (lazy-imported; uncomment to enable in the app)
# paddleocr>=2.7.0
# paddlepaddle>=2.5.0

# Phase 3 — CRM classifier (still stub)
# sentence-transformers>=2.2.2
# scikit-learn>=1.3.0
# keplergl>=0.3.2
